@model IEnumerable<Product>
<div class="container">
    <div class="row">
        @foreach (var item in Model)
        {
            @await Html.PartialAsync("ProductSummary", item)
        }
    </div>
</div>
